# Node / Obsidian plugin
plugin/node_modules/
plugin/main.js
plugin/*.map

# Python server
server/.venv/
server/__pycache__/
**/__pycache__/
*.pyc

# Models / caches
server/models/
.cache/

# 自動起動時のサーバーログ（.1〜.5 は起動ごとに世代を送った過去ぶん）
server/server.log
server/server.log.*

# 文字起こしモードの保存音声（復旧用。中身は生の会話なので追跡しない）
server/recordings/

# 辞書の安全保存で作る直前バックアップ／一時ファイル
server/dictionaries/profiles/*.json.bak
server/dictionaries/profiles/*.json.tmp

# 精度解析のレポート（会話の中身がそのまま入るので追跡しない）
server/analysis/
