# Phase 1 — ローカルLLMを測る

対象: `docs/REDESIGN_AI_POSTEDIT.md` の Phase 1 / 所要: 準備30分＋実行1〜2時間

自宅PCで走らせる手順。**このリポジトリの外に出るデータは無い**（モデルもローカル）。

---

## 0. なぜ、この測りかたなのか

正解データを作らない。**辞書がそのまま正解**になる。

登録済みの `SMTV → SMTB` は、実際の取材で観測された誤認識と正しい表記の対である。
だから手元のノートに `SMTV` があれば、そこは誤りだと分かっている。
**評価用の文章を書き起こす必要はない** —— 書き起こすと、書いた側の想定しか測れない。

測るのは3つ。

| 問い | 見る数字 |
|---|---|
| **効くか** | 既知の誤りのうち、モデルが直した割合 |
| **危なくないか** | 却下の内訳（読み・数値・語の創作・欠落） |
| **いくらかかるか** | ブロックあたりの所要時間 |

「悪化しないか」は測らなくてよい。門が止めるので**構造的に悪化しない**。

---

## 1. 準備

### まずブランチを取る

このツールは `main` にまだ入っていない。**ブランチを切り替えないと
`bench_refine.py` が存在しない。**

```powershell
cd <リポジトリ>
git fetch origin claude/wizardly-wright-el1yt0
git checkout claude/wizardly-wright-el1yt0
```

サーバーを常駐させている場合、この測定は**サーバーを止めずに走らせてよい**
（`bench_refine.py` はテキストしか触らない）。ただし辞書の5件を無効化した
コミットを反映するには、サーバーの再起動が要る（`.\server\control.ps1 restart`。
**文字起こし・録音中は実行しないこと**）。

### モデルを入れる

```powershell
winget install Ollama.Ollama
ollama pull qwen3:8b       # 8B。VRAM 8GB 程度から
ollama pull qwen3:14b      # 余裕があれば
ollama list                # 入っているものを確認
```

`ollama serve` が `http://localhost:11434` で待ち受けていれば準備完了。
別のポートや別の実装（llama.cpp のサーバー等）なら `--base-url` で指す。

**思考を出すモデルに注意。** qwen3 系は既定で `<think>…</think>` を吐く。
`refine_llm.py` が剥がすので測定は通るが、**そのぶん生成が遅くなる**
（1ブロックあたり数倍になることもある）。速度を測るなら、プロンプト末尾に
`/no_think` を足すか、思考しないモデルを選ぶ。

**GPUの取り合い。** Whisper と同じGPUに載せると、文字起こし中は両方遅くなる。
測定は認識を回していないときにやる。

### 配線を確かめる（モデル不要・10秒）

```powershell
cd server
.\.venv\Scripts\python.exe bench_refine.py --text <ノート>.md --model stub --dictionary-set default
```

**正しい出力**: 採用100% / 直した量0字 / 「一切直していない」の警告。
既知の誤りが「入力に現れた N 件・直した0件」と出れば、採点も効いている。
ここで N が 0 なら、そのノートには登録済みの誤りが残っていないので測れない
（別のノートを選ぶ）。

### 測るノートを選ぶ

**登録済みの誤りが本文に残っているノート**がよい。0.23.0 のデジ庁会見のノート
（24語を登録しても本文が62箇所そのままだった、あれ）がまさに理想的な対象。

**辞書セットは、そのノートの分野に合わせる。** 決済まわりの取材なら
`--dictionary-set payments`、それ以外は `default`。合っていないと既知の誤りが
1件も見つからず、採点が空振りする。

録音から作り直してもよい（GPUが要る）:

```powershell
.\.venv\Scripts\python.exe bench_refine.py --session 20260806-133113 --limit-sec 900 --model qwen3:8b --dictionary-set default
```

口述の録音は残らない（`recording.py` は文字起こしモードだけ）ので、原稿執筆モードの
入力も当面は会見・取材の生テキストで代用する。自然発話でフィラーも言い直しも入って
いるので、入力の性質としては近い。

---

## 2. 本番（3回まわす）

### ① 原稿執筆モード・用語集なし —— **これが本命**

```powershell
.\.venv\Scripts\python.exe bench_refine.py --text 会見.md `
    --mode manuscript --model qwen3:8b `
    --dictionary-set default --no-glossary `
    --out 校正後.md --diff 変更.md -v
```

用語集を渡さずに、モデルが文脈だけでどこまで直せるかを見る。
**再設計の中心的な問いはここで決まる** —— 語を1件ずつ登録しなくても本文が直るなら、
辞書の「モデルに教える」役割は要らなくなる。

> 注意: 用語集を渡さないのはモデルへの入力だけで、**門の許可証としては使う**
> （語の門は用語集に無い固有名詞を通さない）。外せるのは辞書の役割の一部であって、
> 項目そのものではない。

### ② 原稿執筆モード・用語集あり

```powershell
.\.venv\Scripts\python.exe bench_refine.py --text 会見.md `
    --mode manuscript --model qwen3:8b --dictionary-set default -v
```

①との差が、**用語集を渡す価値**そのもの。

> **生出力は自動で残る。** `--out 校正後.md` を付けると `校正後.md.raw.txt` にも
> モデルの生出力が保存される。門やプロンプトを変えたあとは
> `--replay 校正後.md.raw.txt` で**GPUを回さずに採点し直せる**。
> コンソールの集計も残したいときは `| Tee-Object 結果.txt` を足す。

### ③ 取材モード

```powershell
.\.venv\Scripts\python.exe bench_refine.py --text 会見.md `
    --mode interview --model qwen3:8b --dictionary-set default -v
```

生テキストを正としたまま、表記だけ直せるかを見る。
**採用条件は「違反0件」**。1件でも通れば取材には使えない（口述専用としては可）。

### モデルを並べる

```powershell
.\.venv\Scripts\python.exe bench_refine.py --text 会見.md --dictionary-set default `
    --compare qwen3:8b qwen3:14b gemma3:12b
```

---

## 3. 読みかた

```
■ qwen3:8b  /  モード: manuscript
  採用            38 件（86.4%）
  却下の内訳
    reading-added          4      ← 対話モードなら問題。原稿執筆では出ない
    invented-term          2      ← 用語集に無い固有名詞を持ち込んだ
  直した量        1,204 字 / 35 ブロック
  所要（中央値）  2.31s / ブロック
  ── 既知の誤り（辞書 = 正解） ──
  入力に現れた既知の誤り  62 件 / 24 種
  モデルが直した          41 件（66.1%）  ← 門の前。モデルの実力
  誤ったまま残った        21 件
  門を通って本文が直る    38 件（61.3%）  ← 実際に効く数
```

**必ず2つ並べて見る。**

- **採用率だけを見ない。** 何もしないモデルは全部の門を通るので採用率100%になる
  （`--model stub` がそれ）。「直した量」が0なら、安全に見えるだけで効いていない。
- **「モデルが直した」と「門を通って本文が直る」の差**が、用語集に載せれば取り返せるぶん。
  この差が大きいなら、結論は「辞書は要らない」ではなく「辞書は**許可証**として要る」。

### 判断

| 見るもの | 採用 | 不採用 |
|---|---|---|
| 取材モードの違反（読み・数値・語） | **0件** | 1件でもあれば取材には使わない |
| 既知の誤りを直した割合 | 高いほどよい。**目安5割** | 1〜2割なら、辞書を置き換えられない |
| 悪化（入力に無い既知の誤りを持ち込む） | 0件 | 1件でも出たらプロンプトを疑う |
| 所要／ブロック | 30秒ブロックの生成より十分速いこと | 認識のレイテンシに影響するなら非同期のまま |

**誤変換が半分になっても、捏造が1件でも通るモデルは取材モードでは採用しない。**

### 残存誤変換を人が数える

`--diff` で出した一覧には、変わったブロックと却下されたブロックだけが載る。
既知の誤り以外（辞書に無い誤変換）は機械では数えられないので、ここを目で見る。
そのとき見つけた対は、そのまま辞書の登録候補になる。

---

## 4. 結果を残す

このファイルの末尾に追記していく。次にモデルやプロンプトを変えたとき、
**前後を比べられることのほうが、1回の数字より価値がある**
（`retranscribe.py --compare` と同じ考えかた）。

```
### 2026-__-__  qwen3:8b / manuscript / <ノート名>
入力 N字 / Mブロック
採用 __%  直した量 __字
既知の誤り __/__ 件（__%）  門通過後 __件
却下: reading-added __ / invented-term __ / too-short __
所要 中央値 __s
所見:
```

---

## 測定結果

### 2026-08-28  予備測定（モデル = Claude・原稿執筆モード・用語集なし）

ローカルLLMではない。**方式が成立するかの上限確認**であって、qwen3:8b の代役ではない。
所要時間は測っていない（意味がないため）。入力は擬似的な口述メモ 439字・4ブロック、
辞書は payments。

| | 結果 |
|---|---|
| 門の採用 | 4/4（却下0） |
| 直した量 | 352字 / 4ブロック |
| 既知の誤り | 入力に11件・9種 |
| 同じ出力を取材モードの門へ | **4/4 却下（reading-changed）** ＝ モード分離は実出力でも効く |

**分かったこと。曖昧な漢字語では、AI校正が無条件置換に勝つ。**

同じ本文・同じ辞書で、現行の無条件置換と並べた:

| 箇所 | 種別 | 無条件置換（現行） | AI校正 |
|---|---|---|---|
| 営業利益は現役 → 減益 | 誤り | ✓ | ✓ |
| 四百億円の現役 → 減益 | 誤り | ✓ | ✓ |
| 予診の審査 → 与信 | 誤り | ✓ | ✓ |
| 不良再建 → 不良債権 | 誤り | ✓ | ✓ |
| 債権の蘇生 → 組成 | 誤り | ✓ | ✓ |
| 現役**の行員**（正しい） | 正しい | **✗ 減益の行員** | ✓ |
| **経営**再建の計画（正しい） | 正しい | **✗ 経営債権の計画** | ✓ |
| | | **5/7** | **7/7** |

置換は誤りを直せるが、**同じ語の正しい用法も必ず壊す**。文脈を見ないので、これは
設定では避けられない。AI校正は7/7で、直すべきものを直し、直してはいけないものに
触らなかった。

**固有名詞は用語集が要る。** 直せなかったのは `水尾銀行 → みずほ銀行`、
`一軒米氏 → eKYC`、`ウィンドウズ → Windows` の3件で、すべて固有名詞。プロンプトが
「用語集にあるものだけ」と制限しており、門も用語集に無い固有名詞を通さない。
**これは失敗ではなく設計どおり**で、結論は「辞書は要らない」ではなく次のとおり。

> **辞書は2種類に分かれる。** 曖昧な漢字語（現役・再建・予診・蘇生）は登録から外して
> AI校正に任せる。固有名詞（PayPay・みずほ銀行・eKYC）は登録を残し、**許可証**として使う。

### 辞書の健康診断（実データ・**対応済み**）

`audit_dictionary` を payments 辞書に当てた結果、**それ自体が普通に使われる漢字語**が5件
見つかった。無条件置換は文脈を見ないので、正しい用法まで壊していた:

| 登録 | 壊れていた例 | 状態 |
|---|---|---|
| 予診 → 与信 | 予診票に記入 → **与信票に記入** | 無効化 |
| 余震 → 与信 | 余震が続いている → **与信が続いている** | 無効化 |
| 再建 → 債権 | 経営再建の計画 → **経営債権の計画** | 無効化 |
| 現役 → 減益 | 現役の行員 → **減益の行員** | 無効化 |
| 蘇生 → 組成 | 細胞の蘇生 → **細胞の組成** | 無効化 |

0.23.0 のプレビューは**登録時**の事故を防ぐもので、登録済みのこれらには効かなかった
（毎回の認識で黙って発火していた）。

**削除ではなく `enabled: false` にした。** 実際に観測された誤認識の記録には値があり、
AI校正が扱うべき語の実例そのものだから。理由は `note` に残してあるので、後から
同じ語を登録し直す事故も防げる。旧編集UIは無効項目を表示しないが、
`update_profile_maps` が保全するので、UIから保存しても消えない（往復で確認済み）。

**曖昧でない登録はそのまま。** `県役 → 減益`（ASRの崩れ）と
`不良再建 → 不良債権`（文脈を含む長いキー）は残っていて、いまも効く。
これが「置換に向く登録」と「AI校正に向く登録」の境目になる。

適用後: 有効な置換 140 → 135件、漢字の高リスク **0件**、壊れる文 0件。

### 2026-08-28  qwen3:8b / 原稿執筆・用語集なし / 大阪大学mdoc（24,430字・137ブロック）

**初のローカルLLM実測。** 修正率は測れていない（このノートに登録済みの誤りが
1件も無かった）。それでも門の統計は本物で、2つのことが分かった。

| | 結果 |
|---|---|
| 採用 | 117/137（85.4%） |
| 却下 | 20件 |
| 呼び出し失敗 | 1件（既定タイムアウト120秒に対し中央値52秒。長いブロックが超えたと見られる） |
| 直した量 | 16,882字 / 112ブロック |
| 所要（中央値） | **52.09秒/ブロック**（合計 7,274秒＝2時間） |
| 既知の誤り | **0件（測れず）** |

却下の内訳:

| 理由 | 件数 | 意味 |
|---|---|---|
| `number-invented` | **7** | 入力に無い数値を持ち込んだ |
| `invented-term` | 5 | 用語集にも入力にも無い固有名詞を持ち込んだ |
| `leading-content-lost` | 4 | 先頭が失われた |
| `content-diverged` | 4 | 全体が別物になった |
| `trailing-content-lost` | 3 | 末尾が失われた |
| `too-short` | 2 | 短くなりすぎ |

**分かったこと1: 門は要る。実測で効いた。**
`number-invented` が7件。**qwen3:8b は 137ブロック中7回、数値を書き換えようとした。**
取材ノートでこれが素通りしていたら致命的で、7件とも門が止めている。
「悪化は構造的に起きない」が実データで裏付けられた。創作系（数値7＋語5＝12件）と
欠落系（13件）で、却下20件のほぼ全部が説明できる。

**分かったこと2: 8Bの原稿執筆モードは、まだ荒い。**
入力24,430字に対し直した量16,882字。`_edit_size` は粗い見積り（先頭と末尾の
共通部分を除いた長さ）なので過大に出るが、それでも書き換えすぎの疑いが強い。
`content-diverged` と欠落系が13件出ているのは、モデルが要約に倒れている兆候。
プロンプトを締めるか、モデルを上げるか、原稿執筆モードの権限を狭めるかの判断が要る。

**分かったこと3: 速度はリアルタイムに使えない。**
中央値52秒/ブロックは、思考ぶんが乗った数字。30秒の音声ブロックを52秒かけて
校正するので、**Phase 3（口述中の自動校正）はこの構成では成り立たない**。
Phase 2（停止後の一括）なら問題ない。`/no_think` での再測が要る。

**この測定の失敗: ノート選びを先に確かめなかった。**
2時間使って主指標が出ていない。同じことを繰り返さないよう、`--scan` で測れる
ノートを探せるようにし、既知の誤りが0件なら本番を**開始前に中止**するようにした
（`--allow-unscored` で従来どおり）。

#### 出力を読んで分かった、いちばん重い1件

集計には出ない。**幻覚が1件、門を全部すり抜けて採用されていた。**

```
生: デジタルインシャフリー は今761万ダウンロードされていて
校: デジタル著作権管理     は現在761万ダウンロードされており
```

「デジタルインシャフリー」は**デジタル認証アプリ**の誤認識。それが DRM に化けた。
なぜ止まらなかったか:

| 門 | 結果 |
|---|---|
| 読み保存 | **原稿執筆モードでは開けていない**（フィラー除去のため） |
| 数値 | 761万は保たれている → 通る |
| 語の創作禁止 | 出力側で見えた語は `マイナ / 東京 / ポケモン` だけ。**「デジタル著作権管理」は普通名詞の合成なので視界に入らない** |
| 欠落 | 十分に重なっている → 通る |

実際に同じ前後で試すと、**取材モードなら `reading-changed` で即座に却下**される
（でじたるいんしゃふりー → でじたるちょさくけんかんり）。

**結論: 原稿執筆モードを取材ノートに使ってはいけない。**
このノート（デジ庁マイナアプリ会見）はまさに取材素材で、モードの選択を誤っていた。
言い換えると、これは門の失敗ではなく**モードの設計どおりの帰結**であり、
取材モードが存在する理由そのものを実データが裏付けた。

同時に、原稿執筆モードの保証が構造的に弱いことも分かった。読みの門を開けると、
残るのは数値・固有名詞・欠落だけで、**普通名詞でできた幻覚は原理的に捕まらない**。
語の門を複合名詞まで広げれば捕まるが、原稿執筆モードは言い換えを許すので
正当な書き換えまで落ちる。ここは緩められない —— **原稿執筆モードは
「自分が書いた文章を自分で読み返す」前提でしか使えない。**

#### そのほか、出力から読めたこと

- **8B は壊れた固有名詞を直せない。** `マイナブル` `マルポータル` `レジンアプリ`
  `デビューアプリ` `エイトキス` はすべて残った。用語集が要るという予備測定の
  結論と一致する。
- **数値の門に偽陽性が1件。** ブロック26で `事業者のメリット121ページ2` という
  連結を、モデルは `メリット1、21ページに…メリット2` と**正しく割った**のに、
  入力に無い 1 と 21 が現れたとして却下された。安全側の失敗なので急がないが、
  連結数字の分割は正当な訂正である。
- **YAML フロントマターを投げていた。** ブロック1がそれで、モデルが長考して
  タイムアウトし1ブロック無駄になった（`strip_frontmatter` で対処済み）。
- **英語の幻覚が入力に混ざっている。** `I'm going to show you a little bit about
  the app...` `They can help us out. We have a service.` は Whisper 側の幻覚。
  校正では直せない（音に無い）ので、認識側の課題として別に残る。

### 次

1. **このノートを取材モードで回し直す。** 上の DRM 件が却下されるはず。
   同じ素材でモードを変えるだけなので、比較として最も価値が高い。
2. `--scan` で測れるノートを選び直して、**修正率を取る**（本命の数字）。
3. `/no_think` で速度を測り直す。


---

## 測定4: qwen3:8b / 取材モード / 同じ素材（2026-08-29）

測定3と**同じノート**（デジ庁マイナアプリ会見）を、モードだけ変えて回した。

```
■ qwen3:8b  /  モード: interview
  ブロック        16 件（3,541 字）
  採用            10 件（62.5%）
  却下            6 件
  呼び出し失敗    4 件
  却下の内訳
    reading-changed        6
  直した量        73 字 / 3 ブロック
  所要（中央値）  65.63s / ブロック（合計 1049.4s）
  既知の誤りが入力に1件も無い（このノートでは測れない）
```

### 読み取り

**1. 門は設計どおりに効いた。却下6件は全部 `reading-changed`。**
測定3で素通りした `デジタルインシャフリー → デジタル著作権管理` のような
読みを変える書き換えが、取材モードでは例外なく止まる。予告どおりの結果で、
取材モードの安全保証はデータで裏付けられた。

**2. しかし、8B は取材モードで実質なにもしていない。**
3,541字に対して**直した量は73字・3ブロックだけ**。採用された10件のうち7件は
無変更で通っている。つまり「読みを変えずに表記だけ直す」ことが8Bにはできず、
何かしようとすると必ず読みごと変えて却下される。

これはベンチが暴くために作った失敗の形そのもの ——
**何もしないモデルは、すべての門を通る。** 採用率62.5%という数字は
安全性の証明であって、有用性の証明ではない。

**3. 速度がさらに落ちた。** 中央値65.63秒/ブロック（測定3は52.09秒）。
呼び出し失敗が1→4件に増えたのは、既定の120秒タイムアウトに当たったため。

**4. 修正率は今回も取れていない。** このノートに登録済みの誤りが無い。
本命の数字は依然として未測定。

### 結論（測定3と合わせて）

| | 原稿執筆モード | 取材モード |
|---|---|---|
| 安全性 | 弱い（普通名詞の幻覚が通る） | **強い**（読み保存で全部止まる） |
| 有効性 | 書き換えすぎ | **ほぼ何もしない** |

qwen3:8b は、**どちらのモードでも実用水準に届いていない**。
門の側は両モードとも意図どおり動いており、残る変数はモデルである。
