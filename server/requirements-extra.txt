# VoxCraft 精度向上オプション（任意）。
# 最小構成で動作確認したあとに追加する。
#
#   pip install -r requirements-extra.txt
#
# - silero-vad + torch: 息継ぎ検出の精度向上（無いとエネルギーVADにフォールバック）
#
# ※ sudachipy は句読点の自動付与に必須になったため requirements.txt へ移動した。

silero-vad>=5.1
onnxruntime>=1.17
torch>=2.2
