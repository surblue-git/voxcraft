# VoxCraft 認識サーバー — まず動く最小構成。
# Python 3.10–3.12 推奨（faster-whisper / ctranslate2 のホイール都合）。
#
#   pip install -r requirements.txt
#
# これだけで音声入力は動く（VADは簡易エネルギー版）。
# 精度を上げたい場合は requirements-extra.txt も入れる。

fastapi>=0.110
uvicorn[standard]>=0.29
numpy>=1.24
faster-whisper>=1.0.3

# 句読点の自動付与（既定ON）と、変換戻しの漢字→読み復元に使う。
# 無くても動くが句読点が入らなくなるため、既定機能として最小構成に含める。
sudachipy>=0.6.8
sudachidict-core>=20240409
