# Changelog

## 0.23.0 - 2026-08-23

**テーマ: 辞書が、目の前のノートまで届くこと。**

辞書は認識のときにしか効かない。だから語を登録しても、いま開いているノートは
直らないままだった。直るのは次に同じ語を録ったときで、その一周には次の取材が要る。
2026-08-21のデジ庁会見のノートで、その場で24語を覚えさせても本文が62箇所そのまま
だったのが発端。

### このノートに辞書を当てる（新コマンド）

育てた辞書を、既にあるノートへ当て直す。選択があればその範囲、無ければノート全体
（手書きの要約は誤認識を含まないので、実測で0箇所しか触らない）。

- 押す前に**変わる箇所の総数・登録ごとの件数・実際の文脈**が出る。行ごとに外せる。
- 適用は1トランザクション。取り消しは `Ctrl+Z` 一回。
- 置換の意味論はサーバーと同じ（元テキストを1回走査／長いキーが先／置換結果は
  再走査しない）。並び順はサーバーが `GET /dictionaries/sets/{set_id}/replacements`
  で適用順のまま配り、プラグイン側では並べ替えない。実測で、置換結果はサーバーの
  `ReplacementPlan` と1文字も違わない。

**件数は「1回の走査で実際に当たった数」で数える。** キーごとに独立して数えると、
包含関係のある登録（`デジタル認識アプリ` と `デジタル認識`）を二重に数えてしまう。
同じノートで、独立に数えると78箇所、実際に変わるのは62箇所だった。

### 登録する前に、何箇所どう変わるかを見せる

辞書は無条件置換なので、危ないのは**誤りを直すつもりで正しい語を潰す**登録。
要約に書いた略称「マイナカード」を正解として登録すると、本文の
「マイナンバーカード」29箇所が消える。クイック登録の画面が、入力しながら
そのノートへの影響を出すようにした。正しい表記が元の表記を3文字以上まとめて
縮めているときは、省略の疑いとして警告する。

### 登録する語を探す（`server/dictcandidates.py`、CLI）

取材ノートでは、正解は既に同じファイルの中に手で書かれている ——
文字起こし本文とは別に、記者が要約を書くから。その2つを突き合わせて候補を作る。
21,554字から121件へ。要約に出てくる略称と言い換えは門で落とす。
`--prompt` で外部LLM用の照合プロンプトを書き出せる（音声は渡さない）。
`--apply` は答えを検証してから通す（特に、正解側に無い綴りの創作を落とす）。

## 0.22.0 - 2026-08-11

**テーマ: 辞書は、書く場所である前に見える場所であること。**

Androidで辞書を開くと項目が1件しか見えず一覧性がゼロ、という報告から。
原因は編集欄を出しっぱなしにしていたこと。1件がキー欄・正解欄・削除ボタンで
3行分の高さを占め、そこへ 40vh の入れ子スクロールが重なっていた。

### 一覧は畳んで、叩いた行だけ開く

`誤認識1、誤認識2 → 正しい表記` の1行に畳んだ。実測 41px/行（従来は約140px）。
375px幅で **2行 → 15行**。詰まったときは誤認識キーの末尾を省略するが、
**正しい表記は削らない**（その行が何の行かを決めているのは正解のほうなので）。
行を叩くと、その行だけラベル付きの編集欄が開く（横幅いっぱい）。

### 探せるようにした。保存を追いかけなくてよくした

- 各セクションに絞り込み欄。登録済みかどうかを確かめてから追加できる。
- 「保存」「キャンセル」を下端に貼り付けた。一覧が長くても末尾まで送られない。
- モバイルでは入れ子スクロールをやめ、モーダル本体のスクロールに任せる
  （40vh の枠の中で指を滑らせるより、画面の高さをそのまま使うほうが速い）。
- 書き方の説明は畳んだ。開いた瞬間に一覧が見えることを優先する。
- 「＋ 追加」はモバイルで横幅いっぱい。押すと新しい行が開いた状態で足され、
  絞り込み中でも隠れないように絞り込みは解除する。

## 0.21.0 - 2026-08-09

**テーマ: 辞書に登録できる単位を、自分で決められるようにする。**

「確定して辞書に登録」が使えない場面がある、という報告から。誤認識は文節をまたぐ:

    「どうおんいぎご」と発話 → 『どの意義語』
    「ど」を叩くと対象は『どの』だけ → 「同音異義語」として登録できない

読みが どのいぎご まで崩れているので、**揺らしでも候補でも届かない**
（0.20.3 で直した濁点1つのケースとは別物）。この場合に必要なのは、
正しい候補ではなく**登録できる単位**だった。

### 候補モーダルに「範囲を広げる」

押すたびに隣の文節を1つ取り込んで開き直す。`どの` → `どの意義` → `どの意義語の`。
広げた範囲がそのまま、置換の対象にも**辞書に登録するキー**にもなる。
広げた先は1つの塊として扱うので、読みの揺らしも範囲全体の読み直しとして効く。

### 言い直しが通ったら、その場で辞書に登録できる

言い直しが成功した瞬間は **(誤り, 正解) の対が手に入る唯一の場所**。
候補モーダル経由では作れない対（読みごと外れていて正解が候補に出ない場合）が、
ここでだけ作れる。置換後に登録を尋ねる（黙っては登録しない。言い直しは
一度きりの言い換えのこともあるため）。

これで「叩く → 候補に無い → 範囲を広げる → 言い直す → 登録」まで繋がる。

## 0.20.3 - 2026-08-09

**テーマ: 読みが外れているからこそ、文節は割れる。**

「同音異義語」が何度言い直しても『動音域語』になる、という報告から。原因は2段ある。

**1. 認識側。** 読みは どうおんい**き**ご で、正しい どうおんい**ぎ**ご とは濁点1つ違い。
濁点の有無は音として小さく、モデルの言語的な事前分布のほうが強い。**同じ言葉を
はっきり言い直しても結果は動かない**（音ではなくモデルの偏りで決まっているため）。

**2. 修正側。ここに直せるバグがあった。** 濁点の揺らし（`variant_candidates`）は
この誤りのために作ったもので、実際 どうおんいきご → どうおんいぎご →「同音異義語」に
到達できる。ところが発火条件が `len(segments) != 1` で打ち切りだった。
**誤った読みでは Google が2文節（どうおんいき / ご）に割るので、揺らしが
いちばん要る場面でだけ発火しない**という状態だった。

- 判定を文節数ではなく**長さ**で行うようにした。割れていた場合は畳んで、
  全体を差し替える候補として並べる。
- タップ経路では、**叩いた文節の読みだけ**を揺らす（'どうおんいき' →
  'どうおんいぎ' →「同音異義」。前半だけ直せば「語」と合わさって全体が直る）。
- 「揺らした読みのかな自体を候補にする」のを**短い読み（4字以下）に限定**した。
  「ては→では」では成り立つが、複合語では本命を押し下げるだけだった
  （実測: 正解が10番目 → 9番目、選択経路では7番目 → 5番目）。

**恒久対策は辞書登録。** 候補モーダルの「確定して辞書に登録」を使えば、以後は
認識直後の後処理で自動的に直る。同じ語で繰り返し困っているなら、これが最短。

## 0.20.2 - 2026-08-09

**テーマ: 行き止まりに、出口を付ける。**

### 候補モーダルに「言い直す」を足した

読み自体が誤認識されていると、候補をいくら並べても正解は出てこない。
「変換候補」を『変換個』と聞き取ったら読みは「こ」なので、「こうほ」の候補は
**原理的に現れない**（実測: `個` を叩くと `子は` などが並ぶ）。その行き止まりから
抜ける道として、候補の隣に置いた。押すと閉じて、次の発話1回がその範囲の置換になる。

短い語を言い直しても認識が化けにくいのは、待っている間だけサーバーが
`AsrOptions.word()` に切り替わるため（`initial_prompt` を外す。文脈のない単語1つを
文の一部として解こうとするのを止める。実測: 「きよ」prompt有→`キオ` / 無→`キヨ`）。
言い直した結果がかなだけなら、候補モーダルが自動で開く。

### 「言い直し待ち」が本文で見えるようになった

対象の範囲を塗って示す。ステータスバーには前から文言を出していたが、モバイルでは
ツールバーの下敷きになって気づけなかった。**何が置き換わるか**は本文を見て分かるべき。

停止時に塗りが残る不具合も直した（`clearDictationAnchor` が `this.cm` を捨てた**あと**に
`setPendingRespeak(null)` が呼ばれるため、解除が届いていなかった）。

### 候補モーダルのボタンが画面外に切れるのを直した

既定では右寄せ1行に並ぶので、画面が狭いと（折りたたみ端末を閉じたとき等）
左端から溢れて「確定」が切れていた。折り返して収める。

## 0.20.1 - 2026-08-09

- **音声入力を止めたら、口述の点線も消えるようにした。** タップの判定は録音中しか
  効かないので、停止後に点線が残っていると「叩けるはずなのに反応しない」という
  見た目と挙動の食い違いになっていた。次の録音を始めるまで消えなかったのを、
  セッション終了時（末尾のチャンクが届いたあと）に消すようにした。

## 0.20.0 - 2026-08-09

**テーマ: 誤変換を、叩いて直す。**

同音異義語の誤変換を直すのに、これまでは「範囲を選ぶ → コマンドを呼ぶ」が要った。
モバイルでは口述中にキーボードが出ないので、直したいと思ったら一度口述を止めるしかなかった。

### 口述したテキストを叩くと変換候補が出る

- 口述で入った範囲を覚え、控えめな点線で示す（`plugin/dictated.ts`）。
  **「ここが怪しい」ではなく「ここは叩ける」**の意味。
- **キーボード抑制中（モバイルの口述中）は素のタップ**で候補を出す。そこはどうせ
  打てないので、カーソルを置く価値がほとんど無い。
- **それ以外（デスクトップ・⌨で解除中）は Alt+クリック**を要求する。素のクリックは
  「喋りながら過去を手直しする」ための操作で、アンカー追記式の要なので奪わない。
- 叩いた**1文節だけ**を置換対象にする。窓の全文節を並べると直したい語が分からなくなり、
  確定時に触っていない語まで置き換わる。

**確信度で「怪しい語」を絞る案は捨てた。** 単語ごとの確信度は無料で取れるが、実測では
近接マイクでも毎分6語に印が付き、その語は `'こちら' '型' 'と'` のような**誤りでない語**だった
（実際の誤りは「第1弾」「アネンダ」「動画」）。確信度はアライメントの不確かさを拾っていて、
意味の誤りは指さない。誤りを見つけるのは人間の目のほうが正確なので、機械は範囲を覚えるまでにする。

### 長い文の再変換が無反応だったのを修正

Google CGI は**読みが53字を超えると例外ではなく空配列を返す**（実測: 53字→9文節 / 54字→0）。
分割せずに投げていたので、候補ゼロのまま `online=true` を返し、プラグインは空のモーダルを
開いて何も起きなかった。実測で読み56字以上の文（26文中6文＝23%）が全滅していた。

読みを**形態素境界で**53字以下に割り、並列に投げて連結する。読み125字＝3分割でも
251ms で、1回呼び出し（279ms）と変わらない（`VOXCRAFT_GOOGLE_CGI_MAX_READING`）。
取得に失敗したときは `online=false` を返すので、空モーダルではなく理由が出る。

### 文節が元テキストの位置を持つようになった

`/reconvert` の各文節が `start`/`end`（送ったテキスト上の文字位置）を返す。`offset` を渡すと
その位置を含む文節の番号が `at` で返る ＝ 叩いた語の特定。既存の呼び出しはこのキーを見ないので影響しない。

実装上の難所は**辞書の逆引き**だった。以前は `text.replace(表記, 読み)` でテキストごと
書き換えていたので、その時点で元の文字位置との対応が失われていた。書き換えるのをやめ、
「この範囲の読みはこれ」と記録する形にした（`_Reading.to_spans`）。

不変条件を2つ、テストで固定してある: `text[start:end] == surface` と「連結すると元テキストに戻る」。
この2つが成り立つ限り、候補で置換してもテキストは壊れない。

## 0.14.0 - 2026-08-08

**テーマ: 忘れる設定を、置き場所ではなく存在ごと消す。**

0.13.0 で文字起こしの言語を選べるようにしたが、**取材の前に切り替えるのを忘れる**という
問題が残った。開始時に選択肢を出す案もあったが、毎回出る確認は数週間で読まれなくなり、
「忘れる」が「反射で確定する」に変わるだけで間違いは同じように通る。

### 自動判別を既定にした

純日本語の録音2本（64分・34分／計487チャンク）で誤って英語と判定したのは
**3件（0.4%と0.8%）だけ**で、**3件とも1.6秒未満**、うち1件は日本語認識も空の無音だった。

そこで **2秒未満のチャンクは判定せず、直前のチャンクの言語を引き継ぐ**
（`VOXCRAFT_LANGUAGE_DETECT_MIN_SEC` / 既定 2.0）。誤判定はこれで実質ゼロになる。
逐次通訳では短い相槌が話者交代を挟まないことがほとんどなので、既定言語に倒すより当たる。

**設定は「日本語に固定／英語に固定」という上書きとして残す。** 既定のまま使えば、
日本語の会議でも英語の取材でも逐次通訳でも、何も切り替えずに正しく録れる。

コマンドは増やしていない。使いづらさは言語の軸に固有のもので、音源や連結の
コマンド構成の問題ではなかった。畳むと割り当て済みのホットキーが外れる代償もある。

## 0.13.0 - 2026-08-08

**テーマ: 日本語だと決め打ちするのをやめる。**

逐次通訳の取材（2026-08-07・Mastercard・50.4分）で、**英語が全体の 42.7%（21.1分）**を
占めていた。言語を `ja` に固定したまま録っていたので、その21分は
「アジアティックコーナース」（agentic commerce）のようなカタカナの粥になるか、
運よく英語で出るかが安定しなかった。

### 文字起こしの言語を選べるようにした

- 設定「認識・整形 → 文字起こしの言語」に **日本語（既定）/ 英語 / 自動判別** を追加。
  **口述は常に日本語で、この設定の影響を受けない。**
- 自動判別はチャンクごとに言語を判定する。逐次通訳のように話者が入れ替わる取材向け。
- 英語チャンクには日本語向けの後処理（表記ゆれ辞書・記号読み上げ・和欧間スペース除去・
  自動句読点）を当てない。英語が直るわけではなく、壊す余地しかないため。

### 言語をモード別に持たせた

`AsrOptions.language` を追加し、`config.language`（グローバル env）への固定をやめた。
これまでは英語を試すには env を書き換えてサーバーを再起動するしかなく、
**その間は日本語の口述が巻き添えで壊れていた**。`dictation()` は `None` のままなので
口述の挙動は不変で、これを回帰テストで固定した（`test_asr_options.py`）。

### 判定に大きいモデルを使わない

言語判定専用モデルを追加（`VOXCRAFT_LANGUAGE_DETECT_MODEL` / 既定 `base`）。
実測（英語30・日本語30チャンク / GTX 1660 SUPER）:

| 判定モデル | 所要 | 日英が明確な60チャンク | 全410チャンク |
|---|---|---|---|
| turbo | 1114ms | 英語30/30・日本語30/30 | （基準） |
| base（既定） | **96ms** | 同上 | turboと95.1%一致 |

**日英がはっきり分かれた区間では turbo/small/base/tiny の判定が完全に一致した。**
全チャンクでは 95.1% に下がるが、**食い違う20件はほぼ全部が日英の同居した区間**で、
turbo 側の確率も割れている（0.48/0.50 など）。どちらか一方が正解という区間ではないので、
turbo で判定しても解けるようにはならない。認識そのもの（1272ms）とほぼ同じ時間が
もう一度乗るだけなので、大きいモデルを使う理由がない。
自動判別の上乗せは 1チャンクあたり +96ms（約8%）に収まる。

### 速度のために小さいモデルへ降りるのは見送った

英語なら base.en で 323ms（turbo @en の 1273ms に対し約4倍速）まで落とせるが、
**誤りが固有名詞に集中する。** この取材の主題語 "agentic" は基準10回中 base.en 6 /
small.en 5 しか出ず、`.en` 専用モデルを取っても多言語版と変わらなかった。
文の骨格は正しいのに "Asian Tech Commerce" "Master Code" と化けるため、
引用が目的の取材では turbo @en のまま使う。

なお **同じ turbo なら英語と日本語の速度はほぼ同じ**（英語音声1303ms / 日本語音声1427ms）。
認識時間はエンコーダで決まる固定費なので、言語では動かない。

### PC音声の補正が言語を無視していた（修正）

`emit_refinement` が常に既定言語で解いていたため、**英語の速報を日本語で差し替える**
状態だった。速報と同じ言語で解くようにした。

さらに自動判別のときは、**30秒の窓に日英が同居していたら差し替えそのものをやめる**。
補正は範囲を丸ごと1言語で解き直すので、逐次通訳のように話者が入れ替わる範囲では
必ず半分を壊す。速報はチャンクごとに判定できていて既に正しいのだから、上書きするほうが悪い。
判定できなかったときも、賭けずに速報を残す（`VOXCRAFT_LANGUAGE_MIX_THRESHOLD` / 既定 0.15）。

### 過去の録音を日英に分けて作り直せる

- `server/bilingual.py` を追加。判定材料（言語確率・logprob・反復度）を TSV に残すので、
  採否の規則は後から変えられる。
- **avg_logprob で言語を選んではいけない。** 言語を取り違えたときの壊れ方が反復で、
  反復は次トークンが予測しやすいので logprob はむしろ高く出る。
- **日英が1チャンクに同居した区間は 9.5%。** VADは息継ぎで切るので切れ目が話者交代と
  一致せず、ja側とen側が別々の中身を拾う。片方を選ぶと反対側の発言が消えるため、
  検出して両方残し `⚠MIX` を付ける。
- `server/bench_en.py` を追加（モデル別の速度・精度・固有名詞の取りこぼし）。

## 0.12.0 - 2026-08-06

**テーマ: 既定を、測定が示したほうに合わせる。**

0.11.0 で「遠いマイク（会見・発表会）」という設定を足したが、**名前も既定も
間違っていた**。長い連結が有利なのはマイクが遠いからではなく、Whisper に十分な
文脈を渡すからで、近接マイクでも不利にならない。

### 既定を長い連結にした

- **文字起こしの連結を 4.0/2.0/2.0 → 10/6/4 に変更**（`VOXCRAFT_JOIN_SEC` ほか）。
  短い連結が有利になる録音は、実測で1本も見つからなかった。

  | | 本文 | 段落 | 定型句で破棄 | 認識時間 |
  |---|---|---|---|---|
  | 近接マイクの取材 6.8分・4.0/2.0/2.0 | 2,869字 | 9 | 0件 | 195秒 |
  | 同・10/6/4 | 2,864字 (-0.2%) | 8 | 0件 | **79秒** |

  会見では 定型句幻覚 15件 → 0件（0.11.0 の実測）。近接マイクでも本文はほぼ同じで
  2.5倍速い。チャンクが減るぶん速いのは、Whisper が入力長によらず30秒窓を計算するため。

- **新たにまたぐ「2〜4秒の息継ぎ」は、会見36.8分で41件（全体の3.3%）、近接マイクの
  取材で1件（1.1%）。** 話者交代が埋もれる心配をしていたが、この規模だった。
  段落分けも悪化しない（会見の5分で 7→8段落、取材で 9→8段落）。

- **設定「遠いマイク」を廃止し、コマンドに変えた。** 始める瞬間に選ぶほうが実態に合う。
  - `文字起こし（動画・会議・会見）の開始/停止` … 既定（長い連結）
  - `文字起こし【対面インタビュー・低遅延】の開始/停止` … 新規。表示が 8秒のまとまり
    → 3〜4秒おきになる代わりに、短いチャンクが単体で Whisper に渡る機会が増える

  ステータスは低遅延で始めたときだけ `● 文字起こし中（低遅延）` になる。
  「反応が遅い」と「コマンドを間違えた」を取り違えないため。

- WebSocket の `start` / `resume` は `farMic` をやめて `lowLatency` を受ける。
  `started` も `lowLatency` を返す。
- 環境変数 `VOXCRAFT_NEARBY_JOIN_SEC` / `_HOLD_SEC` / `_BREAK_SEC`（4.0 / 2.0 / 2.0）。
  `VOXCRAFT_FAR_MIC_*` は廃止。

### 判定に使えなかった指標（実測して却下）

「音が悪いから認識も悪い」を自動で警告できないかを検討し、3つ試して3つとも捨てた。

| 指標 | 結果 |
|---|---|
| Whisper の `avg_logprob` | 最悪の発表会 -0.224 が近接マイク -0.271 より「自信あり」。流暢さを測っていて正しさと無関係 |
| 発話終わりの減衰（残響の代理） | 最悪の発表会が最速 9.1 dB/100ms。順序が品質と無関係 |
| 変調の深さ | 良い録音（D3200）のほうが浅い（8.8 vs 10.1 dB） |

品質と順序が一致したのは**音量だけ**（発話レベル p90 で 良 -15.1/-28.7、悪 -33.1/-33.6）。
ただし同じ音声を dynaudnorm でゲインアップすると p90 -12.6 と全録音中で最高になり、
認識は悪いまま。**音量だけを見て警告すると「ゲインを上げれば直る」と誤解させる**ので、
警告を出すなら文言を距離の話にする必要がある。今回は実装を見送った。

### 「発話かどうか」を、録音レベルの絶対値で決めるのをやめた

- **エネルギーVADの床を暗騒音に追従させた**（`VOXCRAFT_ADAPTIVE_VAD`、既定ON、
  文字起こしのみ）。`silero-vad` が入っていない環境では `build_detector` が
  `_EnergyDetector` に落ち、その判定は **RMS 0.016（-35.9 dBFS）の絶対しきい値**
  だった。録音レベルはマイクと距離で20〜30dB動くので、これは簡単に外れる。

  2026-08-06 の会見録音は音声が -42.9 dBFS ＝ 床の7dB下にあり、**12秒のあいだ
  一度も発話と判定されず** `VadChunker._finalize` がバッファごと捨てていた。
  `⟨未認識⟩` が `max_chunk_sec` の整数倍で出るのはこれが理由（2026-07-29 から
  未解明だった件）。実測:

  | 区間 | RMS | 発話と判定されたフレーム |
  |---|---|---|
  | 欠落 14:26–14:38 | -49.7 dBFS | **0/375** |
  | 欠落 32:01–32:13 | -47.6 dBFS | **0/375** |
  | 拾えた 15:00–15:12 | -33.0 dBFS | 111/375 |

  捨てられた区間を後から単独で認識すると**実際の発話が出る**。音は入っていた。

- **固定床より厳しくはならない。** 適応床は固定床を上限に持つので、今まで拾えて
  いた音は必ず拾う。同じ録音・同じ分割条件での前後:

  | | カバー率 | 12秒級の穴 |
  |---|---|---|
  | 会見・遠いマイク | 96.2% → **100.0%** | 6件 → **0件** |
  | 発表会・遠いマイク | 97.0% → 98.0% | 2件 → 2件 |
  | 取材・近接マイク | 100% → 100% | 連結後51チャンクで**完全に不変** |

  暗騒音の何倍を発話とみなすかは 2.0 / 3.0 / 4.0 / 6.0 を実測し、決定的な指標
  （カバー率・12秒の穴・近接マイクの不変性）がすべて同じだったので 3.0 にした。

- **口述には掛けない。** 床が下がる方向に動くので、掛けると吐息を拾って「はい」等
  の幻覚が増える。口述の録音で試すと 137→134 / 78→78 チャンクとほぼ動かないが、
  それでも方針どおり文字起こしだけに限定してある（`test_adaptive_vad.py`）。

- **`retranscribe.py` の復旧区間に `⟨復旧 mm:ss–mm:ss …⟩` の印を付けた**（既定ON、
  `--no-mark-recovered` で無効）。VADが「発話なし」と判断した区間を拾い直したぶん
  なので確度が低く、引用の前に音を確かめられる必要がある。`⟨未認識⟩` と同じ記法
  なのでまとめて検索・除去できる。

## 0.11.0 - 2026-08-06

**テーマ: 会場のマイクを離れた場所から拾った音を、捨てずに文字にする。**

記者会見が「認識が厳しい」と感じていたものの正体は、モデルの限界ではなく連結の
設定だった。定型句の幻覚に関する限り、壊れていたのは音の切り方（VAD）ではない。
実測でVADは音声の 96.2% を拾っている。問題はその後、短いチャンクを単体で Whisper に
渡していたところにあった。

> **0.11.0 公開後の訂正:** 残り 3.8%（36.8分で約84秒）を「無音」と読める書き方を
> していたが、これは誤り。実際には**12秒まるごと落ちた実発話**で、原因は別にある
> （silero-vad 未導入時に落ちる `_EnergyDetector` が、録音レベルによらない絶対RMS
> しきい値 -35.9 dBFS で判定しているため。この録音の音声は中央値 -42.9 dBFS）。
> 上の「連結が主犯」という結論は、同一VAD条件での前後比較なので変わらない。

### 遠いマイク（会見・発表会）

- **設定「遠いマイク（会見・発表会）」を追加した。** 文字起こしモードのマイク入力
  にだけ掛かる（口述とPC音声は一切変わらない）。認識に回す前のチャンク連結を
  「10秒までまとめる / 実時間で6秒まで待つ / 4秒までの息継ぎならまたぐ」に変える。

  実測 2026-08-06（NTT決算会見 36.8分・会場の音をPCのマイクで収録）:

  | 連結の設定 | 連結後のチャンク | 中央値 | 4秒未満 |
  |---|---|---|---|
  | 従来（4.0 / 2.0 / 2.0） | 634件 | 3.2秒 | 64.4% |
  | 遠いマイク（10 / 6 / 4） | 263件 | 8.2秒 | 9.9% |

  記者会見は「間」だらけで、従来値の break 2.0秒（これ以上空いたら繋がない）と
  hold 2.0秒（待つのは実時間で2秒まで）の両方に毎回引っかかる。連結がほぼ素通りし、
  3秒前後のチャンクが単体で Whisper に渡っていた。捨てられた53チャンクのうち
  **47件が4秒未満** ＝ 連結が救うはずだった長さ。

- **同じ10分・同じモデルで、定型句の幻覚が 15件 → 0件。** 認識時間も 511秒 → 116秒。
  Whisper は入力長によらず30秒窓を計算するので、チャンクが減るぶんだけ速くなる。

- **モデルの差し替えでは直らないことを確認した。** 同じ条件で large-v3 に替えると
  15件 → **20件** と悪化する。3秒の音声を30秒窓に入れれば残り27秒は無音パディングで、
  そこに書くべきものは音の中に無い。大きいモデルほど学習データの事前分布（動画の
  アウトロ）を堂々と出す。音量正規化（Whisper は log-mel を入力ごとに正規化するので
  ゲインは素通りする）と `condition_on_previous_text` も、同じ音声で無効だった。

- 表示は最大6秒遅れる。効いているかどうかはステータス（`● 文字起こし中（遠いマイク）`）
  と、サーバーが `started` で返す `farMic` で確認できる。「反応が遅い」と「設定を
  入れ忘れた」を取り違えないため。

### 録音からの作り直し

- **`server/retranscribe.py` を追加した。** 保存済みの録音を、実運用と同じ刻み方で
  もう一度認識し、ノート本文（段落・辞書・句読点・⟨未認識⟩ 込み）を作り直す。
  `--compare` を付けると、同じ音声を従来の連結と遠いマイクの両方で回して並べる。

  これが必要なのは、**幻覚に化けたチャンクは捨てられて何も残らない**ため。
  2026-08-06 の会見4本（約2時間）では 250チャンク・544秒（9分）がそうなっていた。
  ブロックリストが効くのでノートは汚れないが、その9分の発言はどこにも無い。
  音声は残っているので、設定を直して通し直せば取り返せる。

  ChunkJoiner の待ち時間は音声上の位置から求めた擬似時刻で測るので、2時間の録音でも
  実時間を待たずに live と同じ連結判断で回せる（`feed_wav.py` は本物の WebSocket を
  通す代わりに実時間かかる。用途が違う）。欠落区間の自動再認識も実運用と同じ条件
  （音声根拠と `retry_min_logprob`）で行う。これが無いと、live なら埋まっていた
  区間が ⟨未認識⟩ のまま残る（実測: 2時間で 3件 → 15件に増えた）。

### 設定（環境変数）

- `VOXCRAFT_FAR_MIC_JOIN_SEC` / `VOXCRAFT_FAR_MIC_JOIN_HOLD_SEC` /
  `VOXCRAFT_FAR_MIC_JOIN_BREAK_SEC`（既定 10 / 6 / 4）

## 0.10.0-beta.8 - 2026-08-05

**テーマ: 認識された文字列と、意図した文字列のズレを吸収する。**

音声入力で本当に困るのは「正しく言っているのに直せない」状況で、原因は層ごとに
違っていた。綴り（辞書のキーが実際の出力と違う）、位置（記号語が単独チャンクに
ならない）、音（読み自体が外れていて再変換が届かない）。この3層をそれぞれ埋めた。

### 括弧・記号

- **再変換に「読み揺らし」の候補を足した。** 再変換は読みから候補を引くので、
  **読み自体が外れた誤認識には原理的に届かない**（実測: 「では」→『ては』。
  テハ からは デハ の候補が絶対に出ない）。しかも言い直しても調音の癖は再現する。
  そこで濁点・撥音・促音を1箇所だけ揺らした読みも変換して候補に混ぜる。
  `ては` → **では**、`へんか` → **へんかん**（変換）、`かこ` → **かっこ**（括弧）。
  揺らしは決定論的な列挙なのでローカルで完結し、判断はユーザーがする。
- 日本語に無い読み（`てっは` `へんっか` など促音が無声子音の前にない形）は
  生成しない。変換にかけると無意味な候補が返り、**本命を上限から押し出していた**
  （`へんか` の変換候補に「変換」が出なかった実例への対処）。1つの揺らしから採る
  候補も2件までにして、本命が別の揺らしにある場合に備える。
  `VOXCRAFT_RECONVERT_VARIANTS=0` で従来動作。
- **括弧が文中でも入力できるようにした（口述のみ）。** これが「括弧だけ一度も
  入力できない」の本体だった。記号変換は単独チャンク一致でしか効かないが、
  「まる」「てん」が文末で言われて自然に息継ぎが入るのに対し、括弧は**文の途中**で
  言ってそのまま中身を続けるため、単独チャンクに決してならない。実測で落ちた例:
  『いつも鍵かっこ閉じ、ダメですね』『このカギカッコ新バージョンは』『ではかっこ、』。
  照合を綴りではなく**読み**にしたので、鍵かっこ／鍵カッコ／カギカッコ／鍵括弧が
  すべて同じ扱いになる（綴りを1つずつ登録して回る必要がない）。形態素の切れ目でしか
  当てないため「かっこいい」「格好」は当たらず、漢字の「括弧」「確固」も温存する。
  `VOXCRAFT_INLINE_SYMBOLS=0` で従来動作。**文字起こしには一切かけない**
  （会議の発言中の「かぎかっこ」は本物の語なので、書き起こしを黙って書き換えない）。
  カタカナが1語に固まった中だけは形態素の切れ目が無いので、直後が長音・ウ なら
  変換しない（「かぎかっこ・かっこうの許嫁」が『カギカッコー…』と1語になり、
  変換すると本文の「カッコー」を食っていた実例への対処）。
- **「かぎかっこ」が絶対に入力できなかったのを修正。** Whisperの出す綴りが
  『鍵かっこ』『カギカッコトジ』で、組み込みの記号語リスト（ひらがな）と一致して
  いなかった。単独で言えていても記号にならず、読点付きで本文に落ちていた。
  他の記号語と同じく観測した綴りを吸収する（鍵かっこ／鍵括弧／鉤括弧／カギカッコ、
  および閉じ側）。単独の「鍵」は本物の語なので対象外。
- **音声コマンドの助詞「を」が「オ」に化けても通るようにした。**
  「かぎかっことじを再変換」→『カギカッコトジオ再変換』と認識され、コマンドが
  成立せず本文に落ちていた。**誤認識されやすい語ほど命令で直せない**という逆転が
  起きるため、`を`／`オ`／`ヲ`／`お` を許す（「Aを再変換」「Aを言い直し」
  「AをBに修正」）。末尾の命令語が一致することが条件なので本文は巻き込まない。
- **記号読み上げの全角記号が半角に潰れていたのを修正。** `（ ） ！ ？ ： ／` は
  Unicodeの全角ASCIIブロックにあり、記号化した直後の半角正規化で `( ) ! ? : /`
  に変換されていた。正規化を記号化の前へ移した（辞書引きのための半角化は従来どおり）。

### 誤認識をその場で辞書にする

- **認識結果をログに残せるようにした（`VOXCRAFT_LOG_CHUNKS=1`、既定OFF）。**
  記号語もコマンドも判定材料は「Whisperが実際に書いた文字列」なのに、それが
  どこにも残らず、画面を突き合わせないと原因が絞れなかった。後処理の前後を
  並べて出すので、辞書が効いていないのか綴りが違うのかが1行で分かる。
  口述内容がそのまま残るため既定はOFF。
- **再変換の候補に記号を並べた。**「まる」と言ったのに『悪』（わる）になる類は、
  読み自体が変わっているので再変換では絶対に「。」が出てこず、言い直しても調音が
  同じなので同じ結果を繰り返す。短い1文節を再変換するときだけ、候補の末尾に
  「。 まる」「、 てん」「改行 かいぎょう」などを足すようにした。数字キーでも
  音声「N番」でも選べる。
- **選んだ記号は「確定して辞書に登録」で記号語辞書へ入る。** 置換辞書ではなく
  `symbols` に入れるのが肝で、記号語はチャンク全体が一致したときだけ効くため、
  『悪』→「。」を登録しても本文中の「悪循環」は無傷のまま。以後その綴りは
  自動で記号になる。サーバーに `POST /dictionaries/{profile_id}/symbols` を追加。
- **オフラインでも記号だけは選べるようにした。** Google CGI が使えないと
  「Aを再変換」「選択範囲を再変換」は候補ゼロで打ち切っていたが、記号セットは
  ローカルにあるので短い対象なら記号だけ並べて開く。サーバー本体はLANに居るため
  辞書登録もそのまま通る。記号を出せない対象（長文など）は従来どおり打ち切る。

### 安定性

- **`/audio-devices` がサーバーを落としていたのを修正。** 0.10.0-beta.7 で入れた
  デバイス列挙をサーバープロセス内で実行しており、別PCから文字起こし中に設定画面を
  開くと `_portaudiowpatch.pyd` がアクセス違反（0xC0000005）で落ち、**録音中の
  サーバーごと死んでいた**。ネイティブのクラッシュなので try/except では防げない。
  列挙を子プロセス（`python system_audio.py --json`）へ追い出し、落ちてもサーバーは
  生き残るようにした。
- 列挙の走査を `get_device_info_by_index` の1周だけにした。
  `get_loopback_device_info_generator` も中身は同じ走査なので、落ちうるライブラリを
  呼ぶ経路を増やさない。

## 0.10.0-beta.7 - 2026-08-05

- **PC音声の取得先を選べるようにした。** これまで「PC音声の文字起こし」はWindowsの
  既定の出力に決め打ちで、設定にある入力デバイスの選択（`PC音声（この端末）`）は
  別コマンド（`この端末のPC音声を文字起こし`）にしか効いていなかった。既定の出力が
  モニターのままヘッドホンで聴いていると無音を録り続け、5分後の自動停止まで
  気づけない。設定 → VoxCraft →「PC音声（サーバー機）」で接続先ごとに選ぶ。
- 一覧はサーバーの `GET /audio-devices` から取る。ホストAPIをWASAPIに絞って
  同じ物理デバイスがMME/DirectSound/WDM-KSぶん重複しないようにし、ループバック
  （再生音）とマイク等の入力の両方を返す。設定はサーバー側の話なので
  Androidからでも効く。
- 指定したデバイスが見つからない場合は、既定へ黙って戻さずエラーで開始を中止する
  （別の音を延々と文字起こしする「気づけない壊れ方」を避けるため）。
- 既定値の「既定の出力」は録音開始時点のWindows既定出力に追従する。Bluetoothヘッドホン
  を繋いだらそちら、外したらスピーカー、と自動で変わるので普段はこのままでよい。
  常に同じヘッドセットで会議に出るなら固定指定の方が確実、という使い分け。
- **開始から20秒、一度も音が来なければ警告するようにした**（`VOXCRAFT_SILENT_WARN_SEC`、
  0で無効）。取得先を取り違えると自動停止の300秒まで黙って無音を録り続け、気づいた
  ときには何も残っていない。ステータスバーも `⚠ 音が来ていません（サーバー機: …）`
  に変わり、実際に音が届いた時点で通常表示へ戻る。サーバー側で判定するので、
  【サーバー機】【この端末】どちらの取得経路でも同じように効く。
- **PC音声のコマンド名にどちらのPCの音かを明記した。** 旧「PC音声の文字起こしを開始/停止」
  → 「PC音声の文字起こし**【サーバー機】**を開始/停止」、旧「この端末のPC音声を文字起こし」
  → 「PC音声の文字起こし**【この端末】**を開始/停止」。前者は初版（同じPCで動かす前提）の
  名残で、別PCのObsidianから叩くとサーバー機の音が録れてしまうことに名前から気づけなかった。
  コマンドIDは変えていないので、割り当て済みのホットキーはそのまま使える。

## 0.10.0-beta.6 - 2026-08-05

- 言い直し・再変換で**範囲選択が不要**になった。直したい語の中にカーソルを置くだけで、
  その語が対象になる。Android の Obsidian は単語のダブルタップ選択がまともに効かず、
  選択を前提にすると端末によっては使えない機能になっていたため。
  対象は文字種の切り替わりで決める（業績 / に / 起用 / しました）。漢字の直後の
  送り仮名は同じ語として繋げる。推定した範囲は必ず選択表示にして、何が対象に
  なったかを見えるようにした。
- 少し広めに取るのは実害が無いどころか有利で、言い直しの発話が長くなるぶん
  認識が良くなる（実測: 「きよ」単独は崩れるが「業績に寄与しました」は完全に取れる）。
- 言い直しの起動語を2種類に分けた。「ここを言い直し」のような命令とわかる言い方だけ
  カーソル位置を使い、「訂正」のような本文にも出る語は従来どおり選択があるときだけ
  コマンドとして扱う（一般語を起動語にできている担保を壊さないため）。

## 0.10.0-beta.5 - 2026-08-05

- 言い直し待ちの間だけ、認識を単語向けに切り替えるように（`AsrOptions.word()`）。
  口述用の `initial_prompt` は文章向けで、文脈のない単語1つを壊していた。
  実測（kotoba、VADで切った実チャンク）: 「きよ」が **キオ → キヨ** に改善。
  文脈のある発話は prompt の有無で結果が変わらないため、待っている間だけ外す。
- 漢字だった箇所を言い直して**かなだけが返ったとき**、変換候補モーダルを自動で開くように。
  「起用」→言い直し→「きよ」で `キヨ` のまま止まらず、第1候補の「寄与」を選べる。
  元がかなだけの箇所や、変換済みで返ってきた場合は開かない。
- ツールバーに**バックスペース**（1文字削除／選択中はその範囲）を追加。
  キーボード抑制中に「入力キャンセル」以外の削除手段が無かった問題への対処。
  取り違え防止に、一文まるごと削除の方を eraser（消しゴム）アイコンへ変更した。

## 0.10.0-beta.4 - 2026-08-04

- 音声コマンドを読み（かな）でも照合するように。表記が違っても音が合っていれば通る
  （例:「にゅりょくキャンセル」「入力不肌」→「入力キャンセル」「入力復元」）。
  判断が微妙なものは本文に入れたうえで、1タップで取り消して実行できる通知を出す。
  設定「読みでコマンドを拾う」でオフにできる。
- 「Xを言い直し」を追加。選択せずに、直す場所を声で指せる（例:「地域を言い直し」）。
  発話どおりの表記で見つからなければ、読み由来の表記候補でも探す（誤変換された字面に届く）。
- 音声コマンドの先読みを追加。口述の短いチャンクだけ小さいモデル（既定 base）で
  先に読み、確信のある固定句はその場で実行する。実測でコマンドの反応が **約1.2秒短縮**。
  本文の認識経路は従来のまま（`VOXCRAFT_COMMAND_PROBE=0` で無効化）。
- `GET /health` に `commandProbe` / `commandProbeModel` / `commandProbeReady` を追加。

## 0.10.0-beta.3 - 2026-08-04

- ユーザー辞書エディタ（設定→VoxCraft→辞書を開く）で、common以外の辞書プロファイルも編集できるように。プロファイルが複数あるときだけ切り替えドロップダウンを表示する。
- サーバーに `GET`/`POST /dictionaries/{profile_id}/dict` を追加（任意プロファイルの置換・記号語をフラット形式で読み書き）。

## 0.10.0-beta.1 - 2026-08-02

- バージョン付き辞書プロファイルと複数プロファイルの辞書セットを追加。
- 録音セッション単位で辞書スナップショットを固定し、再変換・音声復旧にも同じセットを適用。
- 接続先ごとの辞書セット選択、適用中表示、選択範囲・再変換からのクイック登録を追加。
- 1件登録にリビジョン競合検出、冪等処理、検証、直前バックアップを追加。
- BRAT向けGitHub Release生成をプレリリース対応へ更新。
