# Changelog

## 未リリース（サーバーのみ）

- **`/audio-devices` がサーバーを落としていたのを修正。** 0.10.0-beta.7 で入れた
  デバイス列挙をサーバープロセス内で実行しており、別PCから文字起こし中に設定画面を
  開くと `_portaudiowpatch.pyd` がアクセス違反（0xC0000005）で落ち、**録音中の
  サーバーごと死んでいた**。ネイティブのクラッシュなので try/except では防げない。
  列挙を子プロセス（`python system_audio.py --json`）へ追い出し、落ちてもサーバーは
  生き残るようにした。プラグイン側の変更は無し（配布物は beta.7 と同一）。
- 列挙の走査を `get_device_info_by_index` の1周だけにした。
  `get_loopback_device_info_generator` も中身は同じ走査なので、落ちうるライブラリを
  呼ぶ経路を増やさない。

## 0.10.0-beta.7 - 2026-08-05

- **PC音声の取得先を選べるようにした。** これまで「PC音声の文字起こし」はWindowsの
  既定の出力に決め打ちで、設定にある入力デバイスの選択（`PC音声（この端末）`）は
  別コマンド（`この端末のPC音声を文字起こし`）にしか効いていなかった。既定の出力が
  モニターのままヘッドホンで聴いていると無音を録り続け、5分後の自動停止まで
  気づけない。設定 → VoxCraft →「PC音声（サーバー機）」で接続先ごとに選ぶ。
- 一覧はサーバーの `GET /audio-devices` から取る。ホストAPIをWASAPIに絞って
  同じ物理デバイスがMME/DirectSound/WDM-KSぶん重複しないようにし、ループバック
  （再生音）とマイク等の入力の両方を返す。設定はサーバー側の話なので
  Androidからでも効く。
- 指定したデバイスが見つからない場合は、既定へ黙って戻さずエラーで開始を中止する
  （別の音を延々と文字起こしする「気づけない壊れ方」を避けるため）。
- 既定値の「既定の出力」は録音開始時点のWindows既定出力に追従する。Bluetoothヘッドホン
  を繋いだらそちら、外したらスピーカー、と自動で変わるので普段はこのままでよい。
  常に同じヘッドセットで会議に出るなら固定指定の方が確実、という使い分け。
- **開始から20秒、一度も音が来なければ警告するようにした**（`VOXCRAFT_SILENT_WARN_SEC`、
  0で無効）。取得先を取り違えると自動停止の300秒まで黙って無音を録り続け、気づいた
  ときには何も残っていない。ステータスバーも `⚠ 音が来ていません（サーバー機: …）`
  に変わり、実際に音が届いた時点で通常表示へ戻る。サーバー側で判定するので、
  【サーバー機】【この端末】どちらの取得経路でも同じように効く。
- **PC音声のコマンド名にどちらのPCの音かを明記した。** 旧「PC音声の文字起こしを開始/停止」
  → 「PC音声の文字起こし**【サーバー機】**を開始/停止」、旧「この端末のPC音声を文字起こし」
  → 「PC音声の文字起こし**【この端末】**を開始/停止」。前者は初版（同じPCで動かす前提）の
  名残で、別PCのObsidianから叩くとサーバー機の音が録れてしまうことに名前から気づけなかった。
  コマンドIDは変えていないので、割り当て済みのホットキーはそのまま使える。

## 0.10.0-beta.6 - 2026-08-05

- 言い直し・再変換で**範囲選択が不要**になった。直したい語の中にカーソルを置くだけで、
  その語が対象になる。Android の Obsidian は単語のダブルタップ選択がまともに効かず、
  選択を前提にすると端末によっては使えない機能になっていたため。
  対象は文字種の切り替わりで決める（業績 / に / 起用 / しました）。漢字の直後の
  送り仮名は同じ語として繋げる。推定した範囲は必ず選択表示にして、何が対象に
  なったかを見えるようにした。
- 少し広めに取るのは実害が無いどころか有利で、言い直しの発話が長くなるぶん
  認識が良くなる（実測: 「きよ」単独は崩れるが「業績に寄与しました」は完全に取れる）。
- 言い直しの起動語を2種類に分けた。「ここを言い直し」のような命令とわかる言い方だけ
  カーソル位置を使い、「訂正」のような本文にも出る語は従来どおり選択があるときだけ
  コマンドとして扱う（一般語を起動語にできている担保を壊さないため）。

## 0.10.0-beta.5 - 2026-08-05

- 言い直し待ちの間だけ、認識を単語向けに切り替えるように（`AsrOptions.word()`）。
  口述用の `initial_prompt` は文章向けで、文脈のない単語1つを壊していた。
  実測（kotoba、VADで切った実チャンク）: 「きよ」が **キオ → キヨ** に改善。
  文脈のある発話は prompt の有無で結果が変わらないため、待っている間だけ外す。
- 漢字だった箇所を言い直して**かなだけが返ったとき**、変換候補モーダルを自動で開くように。
  「起用」→言い直し→「きよ」で `キヨ` のまま止まらず、第1候補の「寄与」を選べる。
  元がかなだけの箇所や、変換済みで返ってきた場合は開かない。
- ツールバーに**バックスペース**（1文字削除／選択中はその範囲）を追加。
  キーボード抑制中に「入力キャンセル」以外の削除手段が無かった問題への対処。
  取り違え防止に、一文まるごと削除の方を eraser（消しゴム）アイコンへ変更した。

## 0.10.0-beta.4 - 2026-08-04

- 音声コマンドを読み（かな）でも照合するように。表記が違っても音が合っていれば通る
  （例:「にゅりょくキャンセル」「入力不肌」→「入力キャンセル」「入力復元」）。
  判断が微妙なものは本文に入れたうえで、1タップで取り消して実行できる通知を出す。
  設定「読みでコマンドを拾う」でオフにできる。
- 「Xを言い直し」を追加。選択せずに、直す場所を声で指せる（例:「地域を言い直し」）。
  発話どおりの表記で見つからなければ、読み由来の表記候補でも探す（誤変換された字面に届く）。
- 音声コマンドの先読みを追加。口述の短いチャンクだけ小さいモデル（既定 base）で
  先に読み、確信のある固定句はその場で実行する。実測でコマンドの反応が **約1.2秒短縮**。
  本文の認識経路は従来のまま（`VOXCRAFT_COMMAND_PROBE=0` で無効化）。
- `GET /health` に `commandProbe` / `commandProbeModel` / `commandProbeReady` を追加。

## 0.10.0-beta.3 - 2026-08-04

- ユーザー辞書エディタ（設定→VoxCraft→辞書を開く）で、common以外の辞書プロファイルも編集できるように。プロファイルが複数あるときだけ切り替えドロップダウンを表示する。
- サーバーに `GET`/`POST /dictionaries/{profile_id}/dict` を追加（任意プロファイルの置換・記号語をフラット形式で読み書き）。

## 0.10.0-beta.1 - 2026-08-02

- バージョン付き辞書プロファイルと複数プロファイルの辞書セットを追加。
- 録音セッション単位で辞書スナップショットを固定し、再変換・音声復旧にも同じセットを適用。
- 接続先ごとの辞書セット選択、適用中表示、選択範囲・再変換からのクイック登録を追加。
- 1件登録にリビジョン競合検出、冪等処理、検証、直前バックアップを追加。
- BRAT向けGitHub Release生成をプレリリース対応へ更新。
